// Point this at your deployed backend from the /backend folder.
// During local testing this can stay as-is if you run the backend on :3000.
window.SPENCER_CONFIG = {
  API_BASE_URL: "http://localhost:3000"
};
